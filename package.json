{
  "name": "lockers",
  "version": "1.0.0",
  "description": "A node.js CRUD app for managing locker assignments in the GT Library.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx nodemon --delay 500ms server",
    "lint": "npx standard --fix"
  },
  "nodemonCofig": {
    "watch": [
      "server"
    ]
  },
  "author": "Tyler Jones",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^11.0.0",
    "express": "^4.17.2",
    "helmet": "^5.0.1",
    "mysql": "^2.18.1",
    "mysql2": "^2.3.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "standard": "^16.0.4"
  }
}
